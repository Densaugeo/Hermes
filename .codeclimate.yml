# Save as .codeclimate.yml (note leading .) in project root directory
languages:
  JavaScript: true
# exclude_paths:
- "Hermes/Hermes.js"
# Since CodeClimate doesn't seem to allow adjusting complexity tests
